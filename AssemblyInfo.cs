using MelonLoader;

// Identifies the mod class and metadata so MelonLoader can register it.
[assembly: MelonInfo(typeof(EnhancedTimer.RTATimerMod), "Enhanced Timer", "1.0.0", "Hyonk Tea", "")]

// Associates the mod with this specific game (developer, name) as reported by the loader.
[assembly: MelonGame("Sample Text Studio", "Neon Boost")]